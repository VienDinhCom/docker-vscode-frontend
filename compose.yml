services:
  development:
    # userns_mode: "keep-id"
    build:
      context: .
      target: development
      args:
        UID: ${UID}
        GID: ${GID}
    volumes:
      - ./:/home/user/frontend
    environment:
      - API_URL=https://example.com/api
    ports:
      - '3000:3000'
      - '53000:53000'
    profiles:
      - development

  production:
    # userns_mode: "keep-id"
    build:
      context: .
      target: production
    environment:
      - API_URL=https://example.com/api
    ports:
      - '3000:3000'
    profiles:
      - production
